export default function HomePage() {
    return (
        <main style={{ fontFamily: "sans-serif", margin: "2rem" }}>
            <h1>Babylapse</h1>
            <p>
                Babylapse is a single-deployment blueprint that keeps the key Lapse API semantics while running
                entirely inside one Next.js app.
            </p>
        </main>
    );
}
